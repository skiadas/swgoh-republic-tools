"""SWGOH reviewer web service.

Modules:
    db      SQLite store (guilds, discord links, job log)
    jobs    job runner (regen / refresh / nightly loop)
    app     FastAPI application + admin endpoints
"""
